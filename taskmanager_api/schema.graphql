type Task {
  id: Int!
  title: String!
  due_date: String
  is_done: Boolean!
  created_at: String
}

type Query {
  tasks: [Task]
  task(id: Int!): Task
}

type Mutation {
  createTask(title: String!, dueDate: String): Task
  toggleDone(id: Int!): Task
  deleteTask(id: Int!): Boolean
}
